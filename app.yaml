runtime: nodejs18
env: standard

instance_class: F1

automatic_scaling:
  min_instances: 0
  max_instances: 3

env_variables:
  NODE_ENV: "production"

# GEMINI_API_KEY is set via Cloud Run --set-env-vars (never in this file)
